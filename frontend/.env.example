# Local dev: Vite proxies /api to Django (see vite.config.ts)
# VITE_API_PROXY_TARGET=http://127.0.0.1:8000

# Production static deploy (Render, etc.): full API base URL
# VITE_API_BASE_URL=https://fleetledger-api.onrender.com/api/v1
